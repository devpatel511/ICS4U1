class Test{   double x;   private double y;      public Test()   {      this.x = 0;      this.y = 0;   }      public Test(double x, double y)   {      this.x = x;      this.y = y;   }      public String toString()   {      return "(" + this.x + ", " +  this.y + ")";   }      public double getSlope(Test other)   {      double slope = (other.y - this.y) / (other.x - this.x);      return slope;   }      public Test midpoint(Test other)   {      Test mp = new Test();      mp.x = (other.x + this.x) / 2;      mp.y = (other.y + this.y) / 2;      return mp;   }      public boolean equals(Test other)   {      return roundThreeDecimals(this.x) == roundThreeDecimals(other.x) && roundThreeDecimals(this.y) == roundThreeDecimals(other.y);   }      public double getY()   {      return this.y;   }      public void setY(double num)   {      this.y = num;   }      public static double distance(Test t1, Test t2)   {      double distance = Math.sqrt(Math.pow((t2.x - t1.x), 2) + Math.pow((t2.y - t1.y), 2));      return distance;   }      // helper method   public static double roundThreeDecimals(double num)   {      double numRounded = Math.round(num * 1000.0) / 1000.0;      return numRounded;   }} 